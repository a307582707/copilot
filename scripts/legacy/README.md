# Legacy helper scripts

One-off / optional utilities kept out of the main `scripts/` surface:

- `convert_docs.py` / `docx_to_text.py` — Markdown ↔ DOCX helpers
- `bring-desktop-front.ps1` — Windows focus helper for the desktop window

Preferred desktop installer: `../build-desktop-installer-csharp-wizard.ps1`.
